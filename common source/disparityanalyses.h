#define MAXRAND			32767/*******************************************************************************	Routines to extract information from arrays and matrices********************************************************************************/#ifdef disparityanalyses	#include <stdlib.h>	#include <stdio.h>	#include <time.h>	#include <math.h>	#include <string.h>	long **RawDist(long **mat, int notu, int nchars, int unknown, int inap);	double **PhenDist(long **DMat, int notu);	double **PhDfromData(long **mat, int notu, int nchars, int unknown, int inap);	double **PhDfromDataPlus(long **mat, long *type, int weight, int notu, int nchars, int UNKNOWN, int INAP);	double PhD(long *sp1, long *sp2, int nchars, int unknown, int inap);	double **binarydis(unsigned long **matrix, int nchars);	double **propshared(unsigned long **matrix, int nchars);	double *bootstrapdisparity(double **dmat, long *present, int notu, int reps, double EB);	double *wholecladedisparitytime(double **PWDis, int notu, long **ranges, int onset, int term);	double *wholecladedisparitytaxa(double **PWDis, int notu, long **ranges, int onset, int term);	double **propshared(unsigned long **matrix, int n);	double *accumdisparityparambstest(double *accumdisp, double *mbl, long **omatrix, int *ctype, int *nstates, int *bias, int *maxch, int *depend, int notu, int nchars, int compat, int RUNS, int debug, int UNKNOWN, int INAP);	double *accumdisparityparambstest2(double *accumdisp, double *mbl, long **omatrix, int *ctype, int *nstates, int *bias, int *maxch, int *depend, int notu, int nchars, int compat, int RUNS, int debug, int UNKNOWN, int INAP);	double *wholecladedisparitytaxa2(double **PWDis, int notu, long **ranges, int onset, int term);	#else	extern long **RawDist(long **mat, int notu, int nchars, int unknown, int inap);	extern double **PhenDist(long **DMat, int notu);	extern double **PhDfromData(long **mat, int notu, int nchars, int unknown, int inap);	extern double **PhDfromDataPlus(long **mat, long *type, int weight, int notu, int nchars, int UNKNOWN, int INAP);	extern double PhD(long *sp1, long *sp2, int nchars, int unknown, int inap);	extern double **binarydis(unsigned long **matrix, int nchars);	extern double *bootstrapdisparity(double **dmat, long *present, int notu, int reps, double EB);	extern double *wholecladedisparitytime(double **PWDis, int notu, long **ranges, int onset, int term);	extern double *wholecladedisparitytaxa(double **PWDis, int notu, long **ranges, int onset, int term);	extern double **propshared(unsigned long **matrix, int n);	extern double *accumdisparityparambstest(double *accumdisp, double *mbl, long **omatrix, int *ctype, int *nstates, int *bias, int *maxch, int *depend, int notu, int nchars, int compat, int RUNS, int debug, int UNKNOWN, int INAP);	extern double *accumdisparityparambstest2(double *accumdisp, double *mbl, long **omatrix, int *ctype, int *nstates, int *bias, int *maxch, int *depend, int notu, int nchars, int compat, int RUNS, int debug, int UNKNOWN, int INAP);	extern double *wholecladedisparitytaxa2(double **PWDis, int notu, long **ranges, int onset, int term);#endif