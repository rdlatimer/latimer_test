#ifdef nexus_write	#include <stdlib.h>	#include <stdio.h>	#include <time.h>	#include <math.h>	#include <string.h>	#include <limits.h>				// needed for RAND_MAX calls		void open_output(char *filename);	void close_output();	void write_nexus_file(long **tree, int node, int notu);	void write_nexus(long **tree, int node, int notu);	void write_nexus_string(long **tree, int node, int notu, char *nexus, int *counter);	#else	void open_output(char *filename);	void close_output();	extern void write_nexus_file(long **tree, int node, int notu);	extern void write_nexus(long **tree, int node, int notu);	extern void write_nexus_string(long **tree, int node, int notu, char *nexus, int *counter);#endif